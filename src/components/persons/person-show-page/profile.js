// Node modules import
import React, { Component } from 'react';

export default class Profile extends Component {
	render() {
		return (
			<div className="row profile-section">
				Web Developer (React + Rails). BLUE
			</div>
		);
	}
}
